[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "Oblisq"
version = "0.1.0"
description = "Oblisq: 3D render engine for oblique microscopy data"
requires-python = ">=3.11"
dependencies = [
    "numpy<2.3.0",
    "PyOpenGL",
    "glfw",
    "PyGLM",
    "tifffile>=2026",
    "tkinterdnd2",
    "dask",
    "zarr",
    "imagecodecs"
]

[project.scripts]
oblisq = "oblisq.main:main"

[tool.setuptools.packages.find]
where = ["src"]
